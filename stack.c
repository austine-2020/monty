#include "monty.h"
/**
 * f_stack - fstack prints the top
 * @counter: line_number
 * @head: stack head
 * Return: no return
 */
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
